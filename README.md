# RTOS-CAN-based-Vehicle-Accident-Detection-System
RTOS-CAN Vehicle Accident Detection and Emergency Alert System is a real-time embedded system that uses an MPU6050 sensor to detect vehicle accidents. FreeRTOS manages multiple tasks, while CAN enables communication between two microcontrollers. and the GPS module retrieves the vehicle's location, the GSM module sends an emergency alert message.
